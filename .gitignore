# macOS
.DS_Store

# Local source material (PDFs, Office documents, mail). Reference-only working
# copies, some of them internal. Never committed.
sources/

# Secrets — only .env.example is committed
.env
.env.*
!.env.example
